# CLASS 관찰 실습 앱 (classobs) — Claude Code 작업 지시문

배포 위치: `app.koowoo.kr/classobs`
용도: 대학원 「수업행동분석론」 4~5주차 CLASS 관찰 실습

---

## 0. 한 문장 요약

학생이 접속해 **유튜브 수업 영상을 보면서** CLASS 10개 차원을 클릭으로 채점하면, 결과가 서버에 자동으로 모여 **같은 세션의 모두가 각자 기기에서 같은 비교 화면**을 보고 "어디서 왜 갈렸는지" 토론하는 도구.

---

## 1. 전제 조건

- 수강생 **5명 내외**. 조를 나누지 않는다. 세션 하나에 들어온 사람 전원이 한 화면에서 비교된다
- 동시 접속 최대 6~7명. **성능·확장성·캐싱을 설계 근거로 삼지 말 것.** 단순함이 유일한 최적화 목표다
- **노트북 사용을 전제한다.** 학생 전원이 노트북을 가져온다. 화면 폭 1280px 기준으로 설계한다. 휴대폰 대응은 하지 않는다(다만 창을 좁혀도 레이아웃이 깨져 읽을 수 없게 되지는 않을 것)
- 학생은 IT 숙련도 중하의 직장인 대학원생. 금요일 저녁 수업. **설명 없이 3초 안에 쓸 수 있어야 한다**

---

## 2. 기술 스택

같은 서버의 `/class` (CLASS 퀴즈)가 이미 쓰고 있는 구조를 그대로 따른다. 새로 배울 것을 만들지 말 것.

- **Node 18 + Express** — 같은 서버에 이미 있는 패턴
- **데이터는 JSON 파일.** 데이터베이스를 붙이지 말 것
- **프런트는 바닐라 JS + HTML + CSS.** React·Vue·번들러·npm 프런트 의존성 금지
- **차트 라이브러리 금지.** 그래프는 전부 인라인 SVG로 직접 그린다
- 프런트 파일은 `public/` 아래 두고 Express가 정적 서빙한다

### 포트와 프록시

```
PORT = 3003   // 3002는 class-quiz가 쓴다. 겹치지 않게
```

Express는 `/classobs` 를 베이스 경로로 마운트하고, Apache가 그 경로 전체를 프록시한다. 이렇게 하면 CORS도, 노출된 포트도 필요 없다.

```apache
ProxyPass        /classobs  http://127.0.0.1:3003/classobs
ProxyPassReverse /classobs  http://127.0.0.1:3003/classobs
```

이 설정 블록을 `deploy/apache-classobs.conf` 로 저장하고, README에 적용 방법을 적는다.

### 프로세스 관리

README에 다음을 **반드시** 적는다. 같은 서버의 `/class` 가 재부팅 후 자동 시작 설정이 없어 10개월간 죽어 있었던 전례가 있다.

```bash
pm2 start server.js --name classobs
pm2 save
pm2 startup     # 출력되는 sudo 명령을 반드시 실행
```

---

## 3. 반드시 지켜야 할 도메인 규칙

교육용 도구이므로 CLASS의 채점 규칙을 틀리게 구현하면 존재 이유가 없다. 협상 불가다.

### 3.1 10개 차원 (Pre-K / K-3 판)

`public/dimensions.js` 한 파일에 `DIMENSIONS` 상수로 넣는다. 연령대별 판본이 다르므로 이 파일만 고쳐 교체할 수 있어야 한다.

| 영역 | 약어 | 차원 | 하위 지표 (화면 표시) |
|---|---|---|---|
| 정서적 지원 | PC | 긍정적 분위기 | 관계 · 긍정적 정서 · 긍정적 의사소통 · 존중 |
| | NC | 부정적 분위기 | 부정적 정서 · 징벌적 통제 · 비꼼과 무례 · 심각한 부정성 |
| | TS | 교사 민감성 | 인식력 · 반응성 · 문제의 효과적 해결 · 학생 안정감 |
| | RSP | 학생 관점 존중 | 학생 관심사와 동기 · 자율성과 리더십 지원 · 의미 있는 토론 · 움직임 제한 |
| 교실 조직 | BM | 행동 관리 | 명확한 행동 기대 · 사전 예방적 접근 · 문제행동 재지시 · 학생 행동 |
| | P | 생산성 | 학습시간 극대화 · 루틴 · 전환 · 준비 |
| | ILF | 교수학습 형식 | 효과적 촉진 · 다양한 방법과 자료 · 학생 흥미 · 명확한 학습 목표 |
| 교수적 지원 | CD | 개념 발달 | 분석과 추론 · 창작 · 통합 · 실생활 연결 |
| | QF | 피드백의 질 | 비계설정 · 피드백 순환 · 사고과정 촉진 · 정보 제공 · 격려와 확언 |
| | LM | 언어 모델링 | 잦은 대화 · 개방형 질문 · 반복과 확장 · 자기 및 병행 서술 · 고급 언어 |

### 3.2 부정적 분위기(NC) — 가장 흔한 사고 지점

- 입력받고 **저장하는 것은 원점수**다. 부정성이 클수록 높은 점수, 거의 없으면 1점. 방향 자체는 다른 아홉 차원과 같다(나타난 만큼 높은 점수)
- **정서적 지원 영역 평균을 낼 때만** `8 − NC원점수` 로 역산한다
  `정서적 지원 = (PC + (8 − NC) + TS + RSP) ÷ 4`
- NC 입력 행은 붉은 계열로 구분하고, 옆에 **"원점수 — 부정성이 클수록 높은 점수"** 를 항상 노출한다
- 매트릭스에서는 **원점수와 역산값을 함께** 표기한다. 예: `2 (역산 6)`
- **레이더 차트에는 역산값을 그린다.** 원점수를 그대로 쓰면 그 꼭짓점만 방향이 반대라 도형이 왜곡된다. 라벨은 `NC(역산)`
- **점 그래프에는 원점수를 그린다.** 학생이 입력한 값 그대로 보여야 근거 토론이 된다. 축 아래에 "원점수" 표기
- JSON에 역산값을 저장하지 말 것. 역산은 항상 표시 시점 계산이다

### 3.3 점수 척도

1~7 **정수만**. 슬라이더·소수점 금지. 해석 구간은 1–2 낮음 / 3–5 중간 / 6–7 높음.
채점 근거는 빈도만이 아니라 **깊이 · 빈도 · 지속 시간**을 함께 본다는 안내를 척도 설명에 넣는다.

---

## 4. 세션

세션은 **사람을 나누는 장치가 아니라 회차를 나누는 장치**다. 4주차 실습, 5주차 실습, 내년 실습이 섞이지 않게 한다.

- 세션 코드는 **4자리 숫자**. 칠판에 적어 알려준다
- 세션에 **유튜브 주소**가 담긴다. 그래서 영상을 바꿔도 파일을 고칠 필요가 없다
- 세션 안의 사람은 전원 한 화면에서 비교된다. 조 구분 없음

### 세션 만들기 — `/classobs/new`

비밀번호로 가리지 않는다. 학생이 실수로 하나 더 만들어도 피해가 없고, 관리할 것이 하나 줄어든다.

입력: 제목(예: `2026-2학기 4주차`), 유튜브 주소.
출력: 4자리 코드와 학생용 주소를 크게 표시. **QR 코드는 만들지 말 것**(외부 라이브러리가 필요해진다). 코드만 크게 보여주면 충분하다.

아래에 기존 세션 목록을 최근 순으로 둔다. 제목·날짜·제출 인원수·비교 화면 링크.

---

## 5. 화면

### 5.1 입장 — `/classobs/`

세션 코드와 이름 두 칸. 끝. 코드를 넣으면 세션 제목을 보여줘 맞게 들어왔는지 확인시킨다.

이름은 `localStorage`에 남겨 다시 들어올 때 자동으로 채운다. 같은 이름으로 다시 제출하면 **덮어쓴다**(새 사람이 되지 않는다).

### 5.2 채점 — 영상을 보면서

**레이아웃.** 좌우 2단이다. 노트북 화면에서 **스크롤 없이 영상과 10개 차원이 동시에 보이는 것**이 목표다.

```
┌─────────────────────┬──────────────────────────────────┐
│                     │  정서적 지원                      │
│   영상               │   PC  ①②③④⑤⑥⑦  [근거____][⏱] │
│   (좌측 고정)        │   NC  ①②③④⑤⑥⑦  [근거____][⏱] │
│                     │   ...                            │
│   [10초 뒤로] [🔊]   │  교실 조직 / 교수적 지원          │
│                     │  ─────────────────────────────   │
│   03:41             │  관찰 메모 / 종합 의견            │
├─────────────────────┴──────────────────────────────────┤
│  정서 5.2 · 조직 4.8 · 교수 3.1    8/10 완료   [제출]   │
└────────────────────────────────────────────────────────┘
```

왼쪽 단은 `position: sticky` 로 고정하고, 오른쪽만 필요할 때 스크롤한다. 좌우 비율은 4:6 정도. 창을 좁히면 세로 1단으로 떨어지되, 그 경우에도 영상은 상단 고정으로 남는다.

**영상.** YouTube IFrame API로 임베드한다. 세션의 주소를 쓴다.

- **기본 음소거.** 학생마다 재생 위치가 달라 소리가 겹치면 강의실이 시끄러워진다. 음소거 해제 버튼은 둔다
- `[10초 뒤로]` 버튼
- 임베드가 차단된 영상이면 검은 화면만 뜬다. 그 경우를 감지해 "이 영상은 임베드가 막혀 있습니다. 프로젝터 화면을 보세요" 를 표시한다

**차원 10개.** 영역 3개로 묶어 표시. 차원마다

- 약어 배지 + 차원명 + 하위 지표(작게, 기본 접힘 / `?` 로 펼침)
- **1~7 원형 버튼.** 지름 32px 내외. **키보드 숫자키 1~7로도 입력**되게 한다 — 노트북이므로 마우스보다 이쪽이 빠르다. 현재 포커스된 차원을 시각적으로 표시하고, Tab 또는 ↓로 다음 차원으로 넘어가게 한다
- **근거 입력란** 한 줄. placeholder로 무엇을 적을지 안내 (생산성 → "전환 장면", 개념 발달 → "교사의 질문을 그대로")
- 근거 입력란 옆 **`[⏱]` 버튼** — 누르면 현재 재생 위치가 `[03:41] ` 로 근거 앞에 삽입된다. `getCurrentTime()` 한 번이면 된다. **이 기능이 토론에서 가장 쓸모 있다.** "나는 3분 41초 장면을 봤는데 당신은?"이 바로 된다

**맨 아래 두 칸.** 종이 관찰시트와 같은 구성이다.

- **관찰 메모** — 맥락·특이사항. 2~3줄
- **종합 의견** — 교사에게 전달할 것. 2~3줄. 차원 점수로는 안 잡히는 인상을 적는 자리이고, 비교에서 토론이 가장 잘 붙는다

**하단 고정 바.** 영역 평균 3개 실시간, 진행률, `[제출]`.

**자동 임시저장.** 입력이 멈춘 뒤 2초마다 서버에 `draft` 로 저장하고 `localStorage`에도 복사한다. 네트워크가 끊겨도 입력을 잃지 않고, 복구되면 다시 올라간다.

근거가 비어 있으면 제출 시 경고하되 막지 않는다. 경고 문구는 분명하게 쓴다 — 근거 없이 점수만 모으면 토론이 성립하지 않는다.

### 5.3 비교

제출하면 자동으로 이동한다. **5초마다 폴링**해서 새 제출을 반영한다(WebSocket을 쓰지 말 것 — 이 규모에 과하다). 상단에 "5명 중 3명 제출" 표시.

위에서 아래로 여섯 덩어리.

**(1) 요약 배지**
- **±1 이내 일치율** — 모든 사람 쌍 × 10차원에서 점수 차가 1 이하인 비율. 캡션: "실제 CLASS 관찰자 인증 합격선은 80%"
- **완전 일치율**
- **가장 갈린 차원 Top 3** — 최댓값−최솟값 기준

**(2) 차원별 점 그래프 — 여기가 본진이다**

가로줄 10개. 각 줄은 1~7 축이고 사람들의 점수를 점으로 찍는다. 같은 점수가 겹치면 세로로 살짝 어긋나게 배치한다. 점에 마우스를 올리면(또는 탭하면) 이름이 뜬다.

```
QF  피드백의 질    ·· ·    ·     ·        평균 3.8  폭 4
                 1 2 3 4 5 6 7
```

**불일치 폭이 큰 차원이 위로 오도록 정렬한다.** 뭉친 줄과 흩어진 줄이 즉시 구분되고, 토론을 어디서 시작할지 화면이 정해 준다.

**NC 줄에만 `← 왼쪽이 좋음` 을 붙인다.** 다른 아홉 줄은 오른쪽이 좋은데 이 줄만 반대다. 원점수를 그리기로 한 이상 이 표기가 없으면 반드시 오독한다.

**(3) 레이더 차트**

꼭짓점 10개에 사람마다 선 하나씩 겹쳐 그린다. 선 색은 사람별로 구분하고 범례를 단다. NC는 역산값(3.2 참조).

**영역 3개짜리 레이더를 만들지 말 것.** 꼭짓점이 셋이면 삼각형이라 차이가 뭉개진다. 반드시 10차원이다.

사람 이름을 클릭하면 그 사람 선만 강조되고 나머지는 흐려진다.

**(4) 점수 매트릭스**

행 = 10차원, 열 = 사람. 셀 배경을 구간(1–2 / 3–5 / 6–7)으로 옅게 칠한다. 행 오른쪽에 평균 · 범위 · 불일치 폭. NC 행은 `2 (역산 6)`. 맨 아래 영역 평균 3행.

**(5) 근거 나란히 보기**

차원을 클릭하면(점 그래프·레이더·매트릭스 어디서든) 그 차원의 근거가 **점수 오름차순**으로 나열된다. `3점 — 홍길동 — [03:41] 교사가 정답만 확인하고 넘어감` 형태. 타임스탬프는 클릭하면 영상이 그 지점으로 이동한다.

상단에 토론 프롬프트를 고정한다.

> 점수가 갈렸다면, 서로 **다른 장면을 근거로 삼은 것**인지 **같은 장면을 다르게 해석한 것**인지 먼저 확인하십시오.

**(6) 종합 의견 카드**

맨 아래. 사람별 종합 의견을 카드로 나란히. 관찰 메모가 있으면 그 아래 작게. 점수는 비슷한데 총평이 갈리는 경우가 자주 나오고, 그때가 토론이 가장 깊어지는 지점이다. 비어 있는 사람은 카드를 만들지 않는다.

**인쇄.** 비교 화면을 A4로 인쇄하면 그대로 토론 기록지가 되도록 `@media print`. 탭·버튼·입력창은 숨긴다.

---

## 6. 서버가 죽었을 때 — 비상 경로

**이 절을 빼지 말 것.** 같은 서버에서 전례가 있다.

- 화면 상단에 **연결 상태 점**을 항상 표시한다. 초록 `연결됨` / 빨강 `서버 연결 끊김`
- 서버에 닿지 못하면 채점 화면은 **그대로 동작**한다(`localStorage`에 저장). 제출 버튼이 `[결과 코드 만들기]` 로 바뀐다
- 코드 형식: `CLASSOBS1:<base64url(JSON)>`. 한글이 들어가므로 `TextEncoder` → base64url. `btoa()` 를 한글에 직접 쓰면 깨진다. 근거는 60자, 메모·의견은 150자에서 자른다
- 비교 화면에는 접힌 상태의 **"코드로 비교하기"** 영역을 둔다. 여러 줄 텍스트박스에 코드를 붙여넣으면 서버 없이 같은 비교 화면이 그려진다
- 평상시에는 이 영역이 접혀 있어 학생 눈에 띄지 않는다

---

## 7. 서버 API

전부 `/classobs` 아래. 응답은 JSON.

```
GET    /api/health                      → {ok:true}
POST   /api/sessions                    {title, videoUrl} → {code}
GET    /api/sessions                    → 최근 세션 목록
GET    /api/sessions/:code              → {code, title, videoUrl, createdAt, count}
POST   /api/sessions/:code/submissions  {name, status, scores, memo, comment} → upsert
GET    /api/sessions/:code/submissions  → [...]
```

### 데이터 구조

```
data/
  sessions.json                 // 세션 메타 목록
  submissions/{code}.json       // 그 세션의 제출 배열
```

```json
{
  "name": "홍길동",
  "status": "submitted",
  "scores": {
    "PC": { "s": 5, "ev": "[02:10] 아침 인사, 또래 웃음 반복" },
    "NC": { "s": 2, "ev": "..." }
  },
  "memo": "...",
  "comment": "...",
  "updatedAt": "2026-09-19T10:32:11.000Z"
}
```

- **이름을 키로 upsert.** 같은 이름이 다시 내면 덮어쓴다
- **점수는 항상 원점수로 저장**한다
- 파일 쓰기는 **직렬화**한다(간단한 쓰기 큐 또는 뮤텍스). 5명이 동시에 내도 서로 덮어쓰지 않아야 한다
- 세션 코드는 `[0-9]{4}` 만 허용하고, 정규식으로 검증한 뒤에 파일 경로에 쓴다(경로 조작 차단)
- 입력 길이를 서버에서도 자른다(이름 20자, 근거 200자, 메모·의견 500자)

---

## 8. 데모 데이터

비교 화면 하단에 작게 `[가상 학생 5명으로 데모]`, `[가상 데이터 지우기]`. 리허설과 수업 중 시연에 쓴다. 가상 데이터는 이름 옆에 회색 **"가상"** 배지.

**난수를 균등분포로 뽑지 말 것.** 실제 CLASS 연구의 분포를 따라야 "교수적 지원이 유독 낮다"는 이 도구의 핵심 발견이 화면에 나타난다. 아래 모수로 정규분포에서 뽑아 1~7로 자르고 정수 반올림한다.

| 차원 | 평균 | 표준편차 |
|---|---|---|
| PC | 5.1 | 0.95 |
| NC (원점수) | 1.8 | 0.78 |
| TS | 4.5 | 1.0 |
| RSP | 4.3 | 0.9 |
| BM | 4.8 | 1.1 |
| P | 4.4 | 1.0 |
| ILF | 4.1 | 1.0 |
| CD | 2.9 | 1.2 |
| QF | 2.1 | 1.1 |
| LM | 2.8 | 0.9 |

출처: La Paro, Pianta & Stuhlman (2004), 미국 Pre-K 교실 표본의 차원별 평균·표준편차. 상수 옆에 주석으로 남긴다.

- **관찰자 편향을 일부러 만든다.** 가상 학생마다 −0.6 ~ +0.6의 개인 편향(관대/엄격)을 고정값으로 부여하고 전 차원에 더한다. 그래야 불일치 지표가 의미 있게 나온다
- **교수적 지원(CD·QF·LM)의 개인 간 분산을 1.4배로 키운다.** 실제로 관찰자들이 가장 많이 갈리는 영역이다
- 근거 텍스트도 차원별·점수 구간별 문장 풀(차원당 구간별 3문장)에서 뽑아 채운다. 타임스탬프도 무작위로 붙인다

---

## 9. 디자인

같은 과목의 종이 관찰시트(`reference/CLASS_관찰시트.html`)를 색·타이포·차원 배열의 기준으로 삼는다. 화면과 종이가 따로 놀면 안 된다.

- 남색 `#1b3a5f` 영역 헤더·기본 강조 / 크림슨 `#9b2335` NC 전용 / 골드 `#8a6d2f` 집계
- `Malgun Gothic` 우선, 시스템 sans 폴백
- 장식 최소화. 그라디언트 배경·카드 그림자·애니메이션 금지. 실습 도구이지 랜딩 페이지가 아니다
- **노트북 기준.** 1280px 폭에서 채점 화면이 스크롤 없이 한눈에 들어와야 한다. 정보 밀도를 낮추려고 여백을 크게 잡지 말 것
- 다크모드 불필요

---

## 10. 하지 말 것

- 로그인·회원가입·비밀번호
- 학번·연락처 등 식별정보 수집 (이름이면 충분)
- 실제 Teachstone 매뉴얼 문구나 공식 채점 앵커 전재
- "정답" 표시, 등급 판정("이 교사는 우수합니다"). CLASS는 등급 도구가 아니다
- 차트 라이브러리, 프런트 프레임워크, 데이터베이스, WebSocket

화면 하단에 고정 문구를 넣는다.

> 공개된 CLASS 연구 논문을 바탕으로 교육 목적으로 재구성한 실습 도구이며, 공식 Teachstone 인증 도구가 아닙니다. 저작권은 Teachstone Training, LLC에 있습니다.

---

## 11. 완료 기준

- [ ] `pm2 start` 한 번으로 뜨고, `/classobs` 로 접속된다
- [ ] 세션을 만들면 4자리 코드가 나오고, 그 코드로 들어간 학생 화면에 영상이 뜬다
- [ ] 노트북 1280px에서 영상과 10개 차원이 스크롤 없이 동시에 보인다
- [ ] `[⏱]` 를 누르면 근거 앞에 현재 재생 위치가 박힌다
- [ ] 두 사람이 제출하면 **각자 기기에서** 같은 비교 화면이 보인다 (붙여넣기 없이)
- [ ] NC에 2를 넣으면 정서적 지원 평균이 `(PC+6+TS+RSP)/4`, 매트릭스에 `2 (역산 6)`, 레이더에는 6으로 그려진다
- [ ] 점 그래프가 불일치 폭 순으로 정렬된다
- [ ] 차원을 클릭하면 근거가 점수순으로 나열되고, 타임스탬프를 누르면 영상이 그 지점으로 이동한다
- [ ] 서버를 끄면 상단이 빨간 점으로 바뀌고, 채점은 계속되며, 코드로 비교할 수 있다
- [ ] 같은 이름으로 다시 제출하면 새 사람이 생기지 않고 덮어쓴다
- [ ] 데모 버튼을 누르면 교수적 지원 세 차원이 정서적 지원보다 눈에 띄게 낮게 나온다
- [ ] 비교 화면을 인쇄하면 A4에 읽을 수 있게 담긴다

---

## 12. 작업 순서

각 단계가 끝나면 노트북 폭(1280px)에서 직접 열어 확인하고 넘어갈 것. **한 번에 다 만들지 말 것.**

1. **서버 뼈대** — Express, 포트 3003, `/classobs` 마운트, `/api/health`, JSON 파일 읽기/쓰기(직렬화 포함), 세션 생성·조회
2. **입장 + 채점 화면** — `DIMENSIONS` 상수, 1~7 버튼, 근거, 메모·의견, 자동 임시저장, 제출. **여기가 8할이다. 실제로 쓸 수 있는 수준까지 먼저**
3. **영상** — YouTube IFrame API, sticky 배치, 음소거 기본, 10초 뒤로, `[⏱]` 타임스탬프
4. **데모 데이터** — 5번을 테스트하려면 데이터가 먼저 필요하다
5. **비교 화면** — 요약 배지 → 점 그래프 → 매트릭스 → 근거 → 종합 의견 (레이더는 6번에서). **세션 만들기 화면(`/classobs/new`, 4절)도 이 단계에서 만든다**
6. **레이더 차트** — SVG 직접 그리기
7. **비상 경로** — 연결 상태 표시, 코드 생성·붙여넣기
8. **인쇄 스타일, Apache 설정 파일, README(PM2 자동 시작 포함)**
